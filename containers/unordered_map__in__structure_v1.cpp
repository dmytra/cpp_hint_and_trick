#include <iostream>
#include <map>

// https://t.me/easy_c_plus/230
/*

Что нужно сделать, что бы хранить свою структуру в unordered_map

Чтобы хранить свою структуру в std::map, необходимо выполнить несколько шагов. Важно понимать, что std::map — это отсортированное дерево, которое использует операцию сравнения для упорядочивания элементов. Поэтому для пользовательских структур требуется определить оператор сравнения.

➕ Упорядоченность: std::map хранит элементы в отсортированном порядке, что позволяет легко выполнять операции поиска, вставки и удаления элементов с логарифмической сложностью.

➕ Гибкость: Возможность использовать пользовательские структуры в качестве ключей позволяет моделировать более сложные отношения между данными.

🤔 Как это используется?

Для использования пользовательской структуры в std::map, необходимо:

➕ Определить оператор сравнения для структуры
   */

// Определение структуры
struct Point {
    int x, y;

    // Оператор сравнения
    bool operator<(const Point& other) const {
        // Сначала сравниваем по x, затем по y
        return (x < other.x) || (x == other.x && y < other.y);
    }
};

int main() {
    // Создание map с пользовательской структурой в качестве ключа
    std::map<Point, std::string> myMap;

    // Добавление элементов
    myMap[{1, 2}] = "Point A";
    myMap[{3, 4}] = "Point B";

    // Доступ к элементам
    std::cout << "Point (1, 2): " << myMap[{1, 2}] << std::endl;
    std::cout << "Point (3, 4): " << myMap[{3, 4}] << std::endl;

    return 0;
}

